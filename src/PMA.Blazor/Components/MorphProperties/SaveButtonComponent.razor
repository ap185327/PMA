<MudButton Command="@ViewModel.SaveCommand"
           StartIcon="@Icons.Filled.Save"
           Disabled="Disabled"
           Color="Color.Primary"
           Variant="Variant.Outlined">@Text</MudButton>